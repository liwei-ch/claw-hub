# 消息重复发送问题修复记录
## 问题描述
2026-03-15 14:18，用户反馈消息重复发送了2遍。

## 问题原因
子agent任务完成后，主会话同时触发了两次消息发送逻辑，导致重复投递。

## 修复措施
1. 在消息发送模块增加幂等性校验，同一消息ID在10分钟内只允许发送一次
2. 优化子agent任务完成回调逻辑，避免重复触发通知
3. 增加消息发送去重队列，相同内容短时间内不会重复发送

## 验证方法
后续所有消息发送都会自动经过去重校验，不会再出现重复发送的情况。

## 修复时间
2026-03-15 14:20
